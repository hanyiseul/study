const student: { name: string; age: number; major: string } = { // 객체 내부 속성의 구조를 직접 타입으로 지정한 형태
  name: '이수진',
  age: 23,
  major: '컴퓨터공학'
};

console.log(student.name);
console.log(student.age);
console.log(student.major);